# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'group-planner' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for group-planner

  target 'group-plannerTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'group-plannerUITests' do
    inherit! :search_paths
    # Pods for testing
  end

	pod 'AlamofireImage'
	pod 'Parse'
	pod 'ParseUI'

end
